# knowledge-base

Markdown RAG sources for `services/ai-gateway` `/rag/query` and Hermes `rag_ask`. Person embeddings in `persons_*` are a second corpus (vector search via gRPC), not stored here.

| File | Audience |
|------|----------|
| `architecture.md` | Table-per-language, ports, compose, layers |
| `how-to-run-tests.md` | PowerShell, k6, orchestrator, Blazor |
| `istqb-defect-process.md` | Severity, isolation, managerial reports |
