# docs/gherkin

Gherkin sources of truth for functional, performance, and security cases. Step automation is PowerShell (`Invoke-ManualTest.ps1` / `Invoke-FunctionalRpc.ps1`), not a separate Cucumber JVM. See `docs/README.md` for the 1:1 map.
